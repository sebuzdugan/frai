import { GateResult } from './schema.js';
export declare function renderText(result: GateResult, specPath: string): string;
export declare function renderJson(result: GateResult, specPath: string): string;
